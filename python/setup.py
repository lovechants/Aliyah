from setuptools import setup, find_packages

setup(
        name="aliyah",
        version="0.1.0",
        packages=find_packages(),
        description="ML Training Visualization Tool Hooks",
        author="j",
        author_email="j@07-i.co",
        requires=[],
        install_requires=[],
        )
